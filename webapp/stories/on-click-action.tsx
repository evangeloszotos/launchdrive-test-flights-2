import { action } from "@storybook/addon-actions";

// eslint-disable-next-line import/no-anonymous-default-export
export const onClickAction = { onClick: action("onClick") };
export const onSignInAction = { onSignInAction: action("onSignInAction") };
